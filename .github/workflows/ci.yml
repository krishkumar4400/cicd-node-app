name: node-app

on:
    workflow_dispatch:
    push:
        paths:
            - "server.js"
            - "package.json"
            - "package-lock.json"
            - "Dockerfile"
            - ".dockerignore"
            - ".github/workflows/**"
        branches:
            - main

jobs:
    build-test:
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Code
              #   uses: actions/checkout@v7
              run: echo ""

            - name: Setup NodeJs Runtime Environment
              #   uses: actions/setup-node@v4
              #   with:
              #       node-version: "20" # Specify your required version (e.g., 18, 20, 22)
              #       cache: "npm" # Caches dependencies automatically

              run: echo ""

            - name: Install Dependencies
              run: echo ""

            - name: Run Tests (If Any)
              run: echo ""

    docker-build-push:
        runs-on: ubuntu-latest
        needs: build-test

        steps:
            - name: Checkout Code
              #   uses: actions/checkout@v7
              run: echo ""

            - name: Login to Docker Hub
              run: echo ""

            - name: Build and Push Image to Docker Hub
              run: echo ""
